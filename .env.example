# Copy this file to .env (or set these as real environment variables / via
# systemd EnvironmentFile=) and fill in real values. Never commit a filled-in
# version of this file.

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=you@example.com
SENDER_PASSWORD=your-app-password
RECIPIENT_EMAIL=you@example.com
