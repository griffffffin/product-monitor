# Dev-only tooling — not needed to run the scraper itself, only for
# formatting/linting/type-checking during development.
-r requirements.txt
pytest==9.1.1
black==26.5.1
ruff==0.15.20
mypy==2.2.0
types-beautifulsoup4==4.12.0.20250516
